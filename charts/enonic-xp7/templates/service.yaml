apiVersion: v1
kind: Service
metadata:
  name: {{ include "enonic-xp7.fullname" . }}
  labels:
    {{- include "enonic-xp7.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "enonic-xp7.selectorLabels" . | nindent 4 }}
